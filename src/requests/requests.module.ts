import { Module } from '@nestjs/common';

/**
 * Requests Module
 * Planned: Unified security request hub — VAPT, Bug Bounty, Red Team,
 * CloudSEK, Qualys CVS request lifecycle management.
 */
@Module({})
export class RequestsModule {}
