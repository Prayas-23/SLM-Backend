import { Module } from '@nestjs/common';

/**
 * Integrations Module
 * Planned: CloudSEK API sync, Qualys CVS import, Jira ticketing,
 * Slack/Teams notifications, SSO/SAML connectors.
 */
@Module({})
export class IntegrationsModule {}
